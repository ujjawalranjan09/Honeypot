{
  "buildCommand": "pip install --no-cache-dir -r requirements.txt",
  "rewrites": [
    { "source": "/(.*)", "destination": "/api/index.py" }
  ],
  "functions": {
    "api/**": {
      "maxDuration": 60,
      "memory": 1024
    }
  }
}